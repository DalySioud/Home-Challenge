# Screenshots

Grafana dashboard captures taken during load testing.

These are referenced in [MONITORING.md](../MONITORING.md).

Add the following screenshots here:

1. **overview.png** — The overview row (CPU gauge, memory gauge, active connections, req/s, load avg, entropy)
2. **cpu.png** — CPU usage by mode + per core (during load test)
3. **network.png** — Network traffic, packets/s, TCP socket states (TIME_WAIT)
4. **ssl.png** — SSL/TLS Offloading row (handshake rate, CPU overhead, entropy, TIME_WAIT, FDs)
5. **nginx.png** — Nginx connections + request rate
